#!/bin/bash
# DEPRECATED: Symlinks are now auto-created by the Vite plugin in astro.config.mjs.
# This file is kept as a no-op so existing workflows don't break.
# See: platformSymlinksPlugin() in astro.config.mjs
echo "[info] Symlinks are auto-managed by Vite plugin. No manual setup needed."
