---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import LikeC4Diagram from '../../components/viewers/LikeC4Diagram';
import { discoverPlatforms, buildViewPaths } from '../../lib/platforms.mjs';

export function getStaticPaths() {
  return discoverPlatforms().map((p) => ({
    params: { platform: p.name },
    props: { manifest: p },
  }));
}

const { manifest } = Astro.props;
const viewPaths = buildViewPaths(manifest);
---

<StarlightPage frontmatter={{ title: 'C4 L2 — Containers & Integrations', tableOfContents: false }}>
  <div class="diagram-fullscreen">
    <LikeC4Diagram viewId="containers" platform={manifest.name} viewPaths={viewPaths} client:only="react" />
  </div>
</StarlightPage>
