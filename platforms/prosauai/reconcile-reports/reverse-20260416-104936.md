---
title: Reverse Reconcile Report
platform: prosauai
date: 2026-04-16 10:49
commits_reviewed: 3
auto_noise: 1
patches_proposed: 2
patches_applied: 2
adr_contradictions: 0
---

## Summary

- Walked `origin/develop` (41 commits total, 3 unreconciled)
- Auto-marcado 1 commit como noise: `2918136c` (`chore: untrack .hypothesis/ cache`)
- 0 doc-self-edits
- 2 commits no cluster `code`:
  - `25e63e68` — `docs: clarify YAML→Python field mapping, migration history, seed UUID comments` (3 arquivos de config/migrations)
  - `8915c820` — `feat: upgrade deps, switch to gpt-5.4-mini, fix classifier + outbound addressing` (34 arquivos)
- Aplicados 2 patches em 1 doc (`containers.md`)

## Applied Patches

### Layer: engineering

1. `platforms/prosauai/engineering/containers.md` — diagrama Mermaid: `"OpenAI GPT mini"` → `"OpenAI gpt-5.4-mini (ADR-025)"` (refs: `8915c820`, `25e63e68`)
2. `platforms/prosauai/engineering/containers.md` — tabela Communication Protocols: `OpenAI GPT mini` → `OpenAI gpt-5.4-mini` (refs: `8915c820`)

**Nota:** `blueprint.md` já referenciava `gpt-5.4-mini` corretamente (linha 63) — nenhum patch necessário.

## ADR Contradictions (NOT auto-resolved)

Nenhuma.

## Skipped / Deferred

Nenhum commit deferido. Os 34 arquivos de código do commit `8915c820` (testes, `webhooks.py`, `classifier.py`, `agent.py`, etc.) não geraram patches adicionais: as mudanças são refinamentos internos (correções de classifier, outbound addressing) já cobertos pela arquitetura documentada. `domain-model.md` e `context-map.md` estão precisos em relação ao estado HEAD.

## Fix colateral detectado

O script `reverse_reconcile_apply.py` tinha um bug: múltiplos patches no mesmo arquivo eram aplicados independentemente a partir do arquivo original em disco, fazendo o segundo patch sobrescrever o resultado do primeiro. Corrigido via `content_cache` interno em `apply_patches`. Teste de regressão adicionado em `test_reverse_reconcile_apply.py::test_multiple_patches_same_file_are_chained`.

## Next Steps

- Rerun após acúmulo de novos commits no `develop` (trigger: badge "drift" no portal).
