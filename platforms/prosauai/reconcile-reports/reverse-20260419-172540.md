---
title: Reverse Reconcile Report
platform: prosauai
date: 2026-04-20 17:25
branch_walked: develop
head_sha: 8da2c78e2e8b
commits_reviewed: 55
auto_noise: 14
patches_proposed: 2
patches_applied: 2
adr_contradictions: 0
adr_candidates: 3
research_gaps: 1
---

## Summary

- Ingested 111 novos commits de `origin/develop` (56 auto-marcados por match com epico aprovado; 55 foram para avaliacao).
- Auto-marcados 14 commits como noise:
  - 1 commit trivial identificado pelo classify
  - 13 SHAs adicionais de lockfiles / `.env.example` / `.gitignore` / `tsbuildinfo` que escaparam do classify e foram filtrados manualmente
- Agregado chegou a 281 `code_items` iniciais → 273 apos segunda passada de filtragem de ruido.
- Dos 273, apenas 2 itens geraram drift acionavel em docs L1; os 271 restantes estao cobertos no nivel de abstracao correto (BC, container, aggregate, ADR) pelos reconcile-reports dos epics 007 e 008.
- Aplicados 2 patches na camada `engineering`.

## Applied Patches

### Layer: engineering

1. `platforms/prosauai/engineering/containers.md` — nova linha #13 na Container Matrix: **dbmate-migrate** (one-shot migration runner `ghcr.io/amacneil/dbmate:2`). Executa `dbmate --wait up` contra `apps/api/db/migrations/*.sql` antes do servico `api` subir. Container estava em producao desde epic 006 (production readiness) mas nao figurava na matriz. (refs: `23c92ff`, `2aea1c8`, `859c64b`, `88a88b5`, `ce17469`)
2. `platforms/prosauai/engineering/domain-model.md` — nova subsecao "Tabelas admin-only (epic 008 — ADR-027)": `public.admin_users` (auth + 3 roles), `public.audit_log` (INSERT-only), schema-level grants (`pool_admin` BYPASSRLS / `pool_app` RLS-enforced) e coluna `trace_steps.span_id`. (refs: `0554cd7`, `23c92ff`, `41a035f`, `59b1903`, `9e41fec`)

## ADR Contradictions (NOT auto-resolved)

Nenhuma contradicao detectada entre HEAD e ADRs existentes nesta rodada.

## ADR Candidates (implicit architectural decisions without an ADR)

Roteados pela sentinela `__ADR_CANDIDATE__` na candidate_docs. Nao auto-patchados.

- **`docker-compose.yml`** (4 SHAs) — topologia compose mudou para `Tailscale-only` (FR-030), integrou `dbmate-migrate` como dependency gate do `api`, e reconfigurou healthchecks. Nao existe ADR cobrindo "dev access via Tailscale" ou "migrate-runner strategy". Recomendacao: `/madruga:adr prosauai` com input *"runtime topology — tailscale-only access + dbmate migration gate"*.
- **`docker-compose.override.example.yml`** (1 SHA) — introduziu o padrao dev-override (bindar portas para localhost em dev). Recomendacao: mencionar no mesmo ADR proposto acima ou criar ADR dedicado para "dev ergonomics".
- **`apps/admin/package.json`** (5 SHAs) — stack de forms/validation ampliada com `base-ui/react`, `react-hook-form`, `zod`, `vitest`. Blueprint linha 294 cobre Next.js 15/shadcn/TanStack/Tailwind/Recharts/lucide mas nao tem ADR sobre form stack. Recomendacao: amendar [ADR-010](../decisions/ADR-010-nextjs-admin/) com "form/validation stack = react-hook-form + zod + base-ui" ou criar ADR-030.

## Research Gaps (tech stack drift)

Detectados a partir de manifests de dependencias. `research/tech-alternatives.md` NAO foi auto-patchada (proposta requer contexto humano comparativo).

- **`package.json` (root)** (1 SHA) — declara `packageManager: pnpm@10.33.0`. `blueprint.md:295` ja cita `pnpm gen:api`, mas `research/tech-alternatives.md` nao avalia pnpm vs npm/yarn/bun. Severidade baixa (pnpm e consensual para monorepos com workspaces). Recomendacao: adicionar 1 paragrafo em tech-alternatives.md ou tratar como debito baixo.

## Skipped / Deferred

- **271 code_items sem patch** — implementacoes de detalhe dos epics 007 (admin dashboard) e 008 (admin evolution): admin auth/metrics routes, observability wiring, config parsers, UI components. Cobertura documental suficiente no nivel de abstracao:
  - M15/M16 + relacoes 24-26 em `context-map.md`
  - Linha 12 "Admin API (FastAPI)" + subsecao "Admin API — superficie por dominio" em `containers.md`
  - ADR-027, ADR-028 em `decisions/`
  - Stack admin em `blueprint.md:293-295`
  - Tabelas `traces`/`trace_steps`/`routing_decisions` em `domain-model.md`
- **12 deleted_files** — migrations 001-009 `.sql` + `apps/admin/middleware.ts` + `apps/admin/src/app/admin/layout.tsx`. Foram consolidadas/renomeadas:
  - Migrations 001-009 → formato timestamped (`20260101000001_create_schema.sql` … `20260101000010_upgrade_agents_gpt5_mini.sql`). Nenhum doc L1 referencia pelo nome antigo. Referencias em `epics/005-conversation-core/*.md` e `epics/006-production-readiness/*.md` sao snapshots historicos — nao limpar.
  - `apps/admin/middleware.ts` → substituido por middleware em `apps/admin/src/middleware.ts` com nova logica de cookie auth.
  - `apps/admin/src/app/admin/layout.tsx` → fundido em `apps/admin/src/app/layout.tsx`.

## Next Steps

- **Abrir 1 ADR** para topologia de runtime (Tailscale + dbmate gate) — consolida os 2 ADR Candidates de compose.
- **Amendar ADR-010** ou criar novo ADR para form/validation stack admin (react-hook-form + zod + base-ui).
- **Research Gap pnpm** — tratar como debito baixo; revisitar se migrar package manager no futuro.
- **Rerodar `/madruga:reverse-reconcile prosauai`** apos proximo batch de commits mergeados em `origin/develop` (filtro "drift" no portal sinaliza).

> handoff: `/madruga:pipeline prosauai` — verificar pipeline status e pos-reconcile drift.
