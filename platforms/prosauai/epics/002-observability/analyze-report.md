# Specification Analysis Report — Epic 002: Observability

**Date**: 2026-04-10  
**Artifacts**: spec.md (20 FRs, 12 SCs), plan.md (6 phases, 5 DDs), tasks.md (51 tasks, 10 phases)  
**Branch**: `epic/prosauai/002-observability`

---

## Findings

| ID | Category | Severity | Location(s) | Summary | Recommendation |
|----|----------|----------|-------------|---------|----------------|
| F1 | Inconsistency | HIGH | plan.md (Technical Context) vs tasks.md (T001) | Plan lists `opentelemetry-exporter-otlp-proto-grpc` but spec FR-005 says "OTLPSpanExporter (HTTP)". Plan DD-003 chose gRPC (port 4317), but FR-005 text says "HTTP". Conflicting protocol specification. | Align FR-005 text to say "OTLPSpanExporter (gRPC)" per DD-003 decision. Update spec to match plan. |
| F2 | Inconsistency | HIGH | spec.md FR-012 vs tasks.md T038-T042 | FR-012 says dashboards "documentados como SpanQL queries versionadas em `phoenix-dashboards/README.md`". Tasks T038-T042 create them in `phoenix-dashboards/README.md`. But plan.md Phase 5 also references this location. However, the project structure in plan.md does NOT list `phoenix-dashboards/` directory in either the epic docs or prosauai repo structures. | Add `phoenix-dashboards/README.md` to the project structure in plan.md under prosauai repo root. |
| F3 | Inconsistency | MEDIUM | spec.md (User Story 1, SC-001) vs contracts (§5 Span Hierarchy) | SC-001 and US1 require "7+ spans" in the trace (webhook → parse → route → debounce.append → debounce.flush → provider.send_text → echo.completed). But contracts §5 shows `parse_evolution_message` as auto-generated by FastAPI middleware, not a manual span. The span hierarchy shows `format_for_whatsapp` as a span but acceptance scenarios don't mention it. Inconsistent span count and naming between spec and contracts. | Reconcile span names and count: clarify which are auto (FastAPI, httpx, redis) vs manual. Update SC-001 to list actual expected span names or use "5+ manual spans" for deterministic testing. |
| F4 | Inconsistency | MEDIUM | tasks.md T020 vs spec.md SC-001 | T020 (E2E trace test) asserts "5+ spans" but SC-001 says "7+ spans". The test would pass at 5 spans but the success criterion requires 7+. | Align T020 assertion with SC-001 (7+ spans) or clarify that 5+ spans are the manual spans only and auto-instrumented spans bring the total to 7+. |
| F5 | Underspecification | MEDIUM | plan.md (Phase 4) | Task D3 references span in `prosauai/main.py` (`_flush_echo`) AND `prosauai/api/webhooks.py` (`_send_echo`). The actual location depends on where echo send logic lives in epic 001 codebase. Plan shows uncertainty with "or" notation but tasks T024 also uses ambiguous "echo send path". | Verify actual code location in prosauai repo and pin the exact file path. Remove ambiguity before implementation. |
| F6 | Coverage Gap | MEDIUM | spec.md FR-016 vs tasks.md | FR-016: "Lua script DEVE aceitar payload JSON `{text, trace_context}` mantendo retrocompatibilidade com payload `text` standalone". Tasks T027/T028 rewrite Lua scripts and T031 handles retrocompat. However, there is no explicit test task for verifying the Lua script itself in isolation (Redis-level). T025 tests context round-trip and T026 tests legacy retrocompat, but these test at the Python level, not the Lua level. | Consider if Lua-level testing is needed or if Python integration tests sufficiently cover FR-016. Document decision. |
| F7 | Duplication | LOW | tasks.md T034 vs T011 | T034 (US2 — validate structlog bridge wiring) is essentially verifying work done in T011 (wire `add_otel_context` into `shared_processors`). T034 description says "confirm `add_otel_context` is in `shared_processors`" — this is validation of T011, not new implementation. | T034 could be merged into T011 as a verification step, or kept as explicit validation checkpoint. Low impact either way. |
| F8 | Duplication | LOW | tasks.md T043 vs T024 | T043 (US6 — validate `gen_ai.system="echo"`) explicitly states "should be done in T024". This is a pure verification task that checks T024's output. | Keep as separate validation checkpoint but mark dependency on T024 explicitly. |
| F9 | Ambiguity | MEDIUM | spec.md FR-013 | FR-013 says "estender o `HealthResponse` existente com campo `observability`" and "O status OTel degradado NÃO causa HTTP 503". But it doesn't define WHEN `last_export_success` transitions from `true` to `false` — after 1 failed export? After N consecutive failures? After a timeout? | Define threshold for `last_export_success: false`. Plan R7 says "último batch", but BatchSpanProcessor has retries. Recommend: `false` if last completed export attempt failed, regardless of retries. |
| F10 | Inconsistency | MEDIUM | plan.md (DD-003) vs research.md (R1) | DD-003 chose gRPC (port 4317). Research R1 docker-compose config shows BOTH ports 4317 AND 4318 exposed. But plan.md Phase 2 task F1 says "ports 6006 (UI) + 4317 (gRPC)". Port 4318 (HTTP) is in research but not in plan. | Decide: expose only 4317 (gRPC, chosen protocol) or both. Recommend: only 4317 to avoid confusion. Update research.md or add comment explaining why 4318 is also exposed (fallback). |
| F11 | Coverage Gap | LOW | spec.md FR-020 vs tasks.md | FR-020: "versão do Phoenix DEVE ser pinada: `arize-phoenix[pg]>=8.0,<9.0` no `pyproject.toml` e Docker image com tag correspondente". T001 adds deps to pyproject.toml and T012/F1 adds phoenix to docker-compose. But neither task explicitly mentions pinning the Docker image tag — T012 shows `image: arizephoenix/phoenix:latest` in research.md. | Update T012 description to explicitly pin Docker image tag (not `:latest`). Align with FR-020 requirement. |
| F12 | Underspecification | MEDIUM | spec.md FR-003 vs tasks.md T029/T030 | FR-003 specifies the multi-message pattern: "primeiro append vira parent, appends subsequentes como OTel Links". Tasks T029 (append) and T030 (flush) implement this. But the mechanism for "first append = parent" is underspecified: how does the flush handler know which item is "first" if Redis RPUSH preserves order? Plan says "extract first item's trace_context as parent" (index 0 = first chronologically). This is correct but relies on RPUSH ordering guarantee. | Document the invariant: RPUSH guarantees insertion order, so index 0 is always the first append. This is a Redis guarantee but should be explicitly stated as an assumption. |
| F13 | Ambiguity | LOW | spec.md (Edge Cases) | Edge case "hot reload do uvicorn em dev" says "documentação orienta aceitar warnings em dev ou desabilitar OTel condicionalmente". But research.md R2 shows a guard `if not settings.dev_reload` and plan.md doesn't implement this guard. Quickstart.md troubleshooting says "Normal em dev, ignorar". | Clarify: is the guard implemented or not? If "aceitar warnings" is the chosen approach, remove the `if not settings.dev_reload` suggestion from research. |
| F14 | Constitution Alignment | LOW | Constitution VII (TDD) vs tasks.md Phase 8 | Constitution mandates TDD ("Write tests before implementation"). Tasks T038-T042 (dashboards) have no tests — they're SpanQL documentation, not code. This is correct (documentation isn't code), but no tasks verify the queries execute correctly against Phoenix. | Add optional validation task: "verify SpanQL queries parse correctly" or explicitly document that dashboard queries are untestable without live Phoenix (accepted limitation). |
| F15 | Inconsistency | LOW | plan.md (Project Structure) vs tasks.md | Plan shows `prosauai/observability/sampler.py` is NOT in the project structure (removed during planning — sampling is handled inline in setup.py). But pitch.md task list mentions "B6 — `prosauai/observability/sampler.py`". Tasks.md does NOT have a sampler.py task. | Confirm sampler logic lives in setup.py (per plan DD). Update pitch.md task list if needed (pitch is append-only but can note superseded items). |
| F16 | Terminology Drift | LOW | spec.md vs tasks.md vs plan.md | Echo send function is called `_send_echo` in spec/pitch, `send_echo` in contracts §5 and tasks T024, and `_flush_echo` in plan Phase 4. Three different names for what may be the same or different functions. | Verify actual function names in epic 001 codebase. Standardize naming across all artifacts. |
| F17 | Coverage Gap | LOW | spec.md FR-019 vs tasks.md | FR-019 requires `OTEL_SDK_DISABLED=true` for regular tests. No task explicitly sets up the pytest fixture for `OTEL_SDK_DISABLED` environment. Research R6 shows the fixture pattern but no task creates `conftest.py` or configures `pytest.ini`/`pyproject.toml` for the env var. | Add task or subtask to configure `OTEL_SDK_DISABLED=true` in test configuration (pyproject.toml `[tool.pytest.ini_options]` env section or conftest.py fixture). |
| F18 | Coverage Gap | MEDIUM | spec.md FR-011 vs tasks.md | FR-011: "Phoenix DEVE armazenar dados no Supabase Postgres usando schema dedicado `observability`". Plan Phase 2 mentions task F3 "Migration SQL: CREATE SCHEMA IF NOT EXISTS observability". But tasks.md has NO task for F3 — it only has T012 (docker-compose) and T005 (.env.example). Schema creation is missing from tasks. | Add task for schema creation: either document that Phoenix auto-creates schema on startup (if true) or add explicit migration task. This is a deployment prerequisite. |

---

## Coverage Summary

### Requirements Coverage

| Requirement | Has Task? | Task IDs | Notes |
|-------------|-----------|----------|-------|
| FR-001 (Auto-instrument FastAPI/httpx/redis) | ✅ | T010 (C1-C3 within setup.py) | Covered in configure_observability |
| FR-002 (Manual spans: webhook, route, debounce, echo) | ✅ | T022, T023, T024, T029, T030 | 5 spans across 3 files |
| FR-003 (W3C Trace Context propagation) | ✅ | T027, T028, T029, T030, T031 | Full RPUSH + extract + Links |
| FR-004 (structlog trace_id/span_id injection) | ✅ | T008, T011 | Bridge + wiring |
| FR-005 (Export spans to Phoenix via OTLP) | ✅ | T010 | ⚠ Protocol mismatch (says HTTP, plan uses gRPC) |
| FR-006 (Mandatory span attributes) | ✅ | T010 (Resource), T007 (conventions) | tenant_id in Resource |
| FR-007 (Domain span attributes) | ✅ | T007, T022, T023, T024 | Conventions + usage in spans |
| FR-008 (Head-based sampling) | ✅ | T010 (sampler config) | Via Settings.otel_sampler_arg |
| FR-009 (Zero PII in spans) | ✅ | T021 (test), T007 (conventions) | phone_hash always |
| FR-010 (Phoenix container via docker compose) | ✅ | T012, T037 | Compose + validation |
| FR-011 (Phoenix → Supabase schema observability) | ⚠ Partial | T012 (env vars only) | ⚠ No explicit schema creation task (F18) |
| FR-012 (5 curated dashboards) | ✅ | T038-T042 | SpanQL queries in README |
| FR-013 (Health endpoint with observability field) | ✅ | T035, T036 | Model extension + test |
| FR-014 (Legacy payload retrocompat) | ✅ | T031 (retrocompat), T026 (test) | Graceful degradation |
| FR-015 (gen_ai.system="echo" placeholder) | ✅ | T024, T043, T044 | Span + validation + test |
| FR-016 (Lua script JSON payload) | ✅ | T027, T028 | RPUSH/LRANGE Lua rewrite |
| FR-017 (D0: 12 doc update proposals) | ✅ | T013-T019 | 7 tasks covering 4+ docs |
| FR-018 (ADR-020 published) | ✅ | T002, T003 | Create + mark superseded |
| FR-019 (OTel SDK disabled in tests) | ⚠ Partial | T045 (test setup.py) | ⚠ No conftest/env config task (F17) |
| FR-020 (Phoenix version pinned) | ⚠ Partial | T001, T012 | ⚠ Docker image uses :latest (F11) |

### Success Criteria Coverage

| SC | Has Task? | Task IDs | Notes |
|----|-----------|----------|-------|
| SC-001 (7+ spans in <30s) | ✅ | T020, T022-T024, T027-T030 | ⚠ Test asserts 5+ not 7+ (F4) |
| SC-002 (3 containers healthy <60s) | ✅ | T012, T037 | Compose + validation |
| SC-003 (Phoenix UI on :6006) | ✅ | T012, T037, T051 | Quickstart validation |
| SC-004 (3 msgs → 1 continuous trace) | ✅ | T025, T029, T030 | W3C context + test |
| SC-005 (grep trace_id → all logs) | ✅ | T032, T033, T034 | structlog bridge tests |
| SC-006 (5 functional dashboards) | ✅ | T038-T042 | SpanQL queries |
| SC-007 (Zero PII in spans) | ✅ | T021 | PII regression test |
| SC-008 (Overhead <5ms p95) | ✅ | T046 | Benchmark task |
| SC-009 (130+ tests, ruff clean) | ✅ | T049, T050 | Regression + lint |
| SC-010 (gen_ai.system="echo") | ✅ | T043, T044 | Validation + test |
| SC-011 (4 docs updated, drift 0%) | ✅ | T013-T019 | D0 tasks |
| SC-012 (ADR-020 published) | ✅ | T002, T003 | Create + supersede |

---

## Constitution Alignment Issues

| Principle | Status | Detail |
|-----------|--------|--------|
| I. Pragmatism Above All | ✅ PASS | Phoenix single container is simplest solution within Supabase constraint |
| II. Automate Repetitive Tasks | ✅ PASS | Auto-instrumentation covers 80% of spans |
| IV. Fast Action Over Excessive Planning | ✅ PASS | 6 implementation phases with clear checkpoints |
| V. Alternatives and Trade-offs | ✅ PASS | 5+ alternatives documented in research.md for each major decision |
| VI. Brutal Honesty | ✅ PASS | Phoenix limitations documented (weak log UI, no auth) |
| VII. TDD | ✅ PASS | 10 test tasks, tests before implementation per user story |
| VIII. Collaborative Decision Making | ✅ PASS | 13 decisions documented in pitch.md with rationale |
| IX. Observability and Logging | ✅ PASS | This epic IS the observability principle implemented |

No constitution violations found.

---

## Unmapped Tasks

All tasks map to at least one requirement or user story. No orphan tasks detected.

---

## Metrics

| Metric | Value |
|--------|-------|
| Total Functional Requirements | 20 |
| Total Success Criteria | 12 |
| Total Tasks | 51 |
| Coverage % (FRs with ≥1 task) | **100%** (20/20) — 3 partial |
| Coverage % (SCs with ≥1 task) | **100%** (12/12) — 1 with assertion mismatch |
| CRITICAL Issues | 0 |
| HIGH Issues | 2 |
| MEDIUM Issues | 6 |
| LOW Issues | 10 |
| Total Findings | 18 |
| Ambiguity Count | 2 |
| Duplication Count | 2 |
| Inconsistency Count | 6 |
| Coverage Gap Count | 4 |
| Underspecification Count | 2 |
| Constitution Violations | 0 |

---

## Next Actions

### No CRITICAL issues — safe to proceed to `/speckit.implement`

**Recommended before implementation (HIGH priority):**

1. **F1 — Fix protocol inconsistency**: Update spec.md FR-005 from "OTLPSpanExporter (HTTP)" to "OTLPSpanExporter (gRPC)" to match plan DD-003 decision. This is a documentation fix, not a design change.
2. **F2 — Add `phoenix-dashboards/` to project structure**: Add the directory to plan.md project structure section.

**Recommended but not blocking (MEDIUM priority):**

3. **F4 — Align T020 assertion**: Change T020 from "5+ spans" to "7+ spans" or clarify the distinction (manual vs auto).
4. **F9 — Define `last_export_success` threshold**: Add a note to spec or plan clarifying when the bool transitions.
5. **F11/F18 — Schema creation + Docker pin**: Add task for Supabase schema creation and pin Phoenix Docker image tag.
6. **F12 — Document RPUSH ordering assumption**: Add explicit note about Redis RPUSH order guarantee.
7. **F17 — Add test configuration task**: Ensure `OTEL_SDK_DISABLED=true` is configured in pytest setup.

**Low-priority improvements (won't block implementation):**

8. Fix terminology drift (F16): standardize `_send_echo`/`send_echo`/`_flush_echo` after verifying actual codebase.
9. Clarify hot reload approach (F13): pick one path and document.
10. Remove `sampler.py` reference from pitch.md (F15).

**Overall assessment**: The three artifacts (spec, plan, tasks) are **well-aligned** with high coverage. The 2 HIGH issues are documentation inconsistencies (not design flaws) and can be fixed in <5 minutes. The MEDIUM issues are clarifications that the implementer can resolve during implementation. **Confidence: Alta** — proceed to implementation.

---

handoff:
  from: speckit.analyze
  to: speckit.implement
  context: "Análise pré-implementação concluída: 0 CRITICAL, 2 HIGH (inconsistência protocolo HTTP vs gRPC em FR-005, phoenix-dashboards/ ausente na project structure), 6 MEDIUM. Coverage 100% FRs e SCs. Nenhuma violação constitucional. Recomendação: corrigir F1/F2 (5 min) e proceder com implementação. 51 tasks, ~1055 LOC, ~6 dias."
  blockers: []
  confidence: Alta
  kill_criteria: "Se Phoenix >=8.0 não suportar Postgres backend, ou se OTel auto-instrumentation for incompatível com FastAPI lifespan tasks, a abordagem precisa ser revisada."
